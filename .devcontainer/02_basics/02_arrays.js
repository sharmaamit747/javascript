const marvel_heros = ['Hulk', 'Caption America', 'SpiderMen']
const dc_heros = ['batman', 'shaktiman', 'junierji']

// marvel_heros.push(dc_heros)
// console.log(marvel_heros)

// const allheros = marvel_heros.concat(dc_heros)

// const allheros = [...marvel_heros, ...dc_heros] (Note: ...spread operator)


// console.log(allheros)

// const unflat = [1, 2, 3, [4, 5, 6, [7, 5 , 4], [2, 1]], 9, 11]

// console.log(unflat.flat(3))

// console.log(Array.isArray([5]))

console.log(Array.from(5))